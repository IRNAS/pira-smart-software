RPi.GPIO==0.6.3
future==0.16.0
pyserial==3.4
resin-sdk==1.6.2
# luma.oled==2.3.0  # ca be removed
python-dateutil==2.6.1
picamera==1.13
numpy==1.13.3
pycrypto==2.6.1
requests==2.9.1
paramiko==2.4.2
PyYAML==3.12
tailer
statistics
python-can
iso8601
m2x
# azure metapackage was changed and split into several smaller packages,
# as we dont need it at the moment, we will comment it out, but this should be
# resolved in the future
# azure     
astral
